[package]
name = "deploy"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
skill-index = { path = "../skill-index" }
anyhow = "1"
serde_json = "1"

[dev-dependencies]
tempfile = "3"
